import { existsSync } from "node:fs";
import { join } from 'node:path';

function getConfiles () {

}


async function loadAndCovertEnv(match = 'VITE_') {
  
}


/**
 * Get the environment variables starting with the specified prefix
 * @param match prefix
 * @param confFiles ext
 */
function loadEnv<T = Record<string, string>> () {

}